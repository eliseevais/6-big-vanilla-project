// window.setTimeout(() => {} , 1000);
const  callback = () => {
  alert('hello')
};
window.setTimeout(callback, 1000); // функцию запустит кто-то другой, а не мы

const User = () => {
  return (
    <></>
  )
}